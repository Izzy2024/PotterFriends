// Configuracion local/produccion para la app
// En produccion, ajusta USE_LOCAL_BACKEND=false y API_BASE_URL a tu backend
window.APP_CONFIG = window.APP_CONFIG || {
  USE_LOCAL_BACKEND: true,
  API_BASE_URL: 'http://localhost:3001'
};
